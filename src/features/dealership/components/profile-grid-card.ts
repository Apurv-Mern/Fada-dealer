/** Shared shell for the three Company Profile grid cards (Documents, Contacts, Business Details). */
export const profileGridCardClass =
  "flex h-full min-h-[min(18rem,35vh)] max-h-[min(28rem,55vh)] flex-col";

/** Scrollable list region inside a profile grid card. */
export const profileGridCardScrollClass =
  "min-h-0 flex-1 overflow-y-auto pr-1";
